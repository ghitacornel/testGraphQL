type Mutation {
    createVehicle(brandName: String, modelCode: String!, type: String!): Vehicle
}

type Query {
    findById(id: Int): Vehicle
    findAll: [Vehicle]
}

type Vehicle {
    brandName: String
    id: Int!
    modelCode: String
    type: String
}
